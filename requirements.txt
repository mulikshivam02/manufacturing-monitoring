# Core
numpy>=1.26
pandas>=2.2
scipy>=1.12
scikit-learn>=1.4
statsmodels>=0.14

# Vision (A1) — CPU-friendly here for dev; GPU used only during actual training on Colab
torch>=2.2
torchvision>=0.17
timm>=0.9
opencv-python-headless>=4.9
scikit-image>=0.22

# Machine / cyber anomaly (A3-A7)
xgboost>=2.0

# Dataset download / extraction helpers
requests>=2.31
tqdm>=4.66
patool>=1.12          # .rar extraction (Paderborn)
rarfile>=4.1           # alt .rar reader (needs `unrar` or `unar` binary on PATH)

# Testing
pytest>=8.0
pytest-cov>=5.0

# Config / repro
pyyaml>=6.0
